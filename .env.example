POSTGRES_USER=change-me
POSTGRES_PASSWORD=change-me
POSTGRES_DB=change-me
# В Docker — хост `db`/`redis` (совпадает с именами сервисов в docker-compose).
# Локально (services/bot без Docker) смени db → localhost.
DATABASE_URL=postgresql://change-me:change-me@db:5432/change-me
ADMIN_USER=admin
# СМЕНИ на проде: админка теперь за nginx + TLS, но 8001 не публикуется наружу (expose).
ADMIN_PASS=Admin112008
BOT_TOKEN=change-me
BOT_USERNAME=change-me            # @<username> бота — нужен для deep-link кнопок ЛК
XUI_URL=https://nyxvpnde.port0.org:8165/60fUYD0loNH7UUHqvb/
XUI_USERNAME=change-me
XUI_PASSWORD=change-me
XUI_INBOUND_IDS=4,5
XUI_SUB_URL=https://nyxvpnde.port0.org:2096/sub/{sub_id}
TRIAL_DAYS=3
REMIND_HOURS_BEFORE=20
SUBSCRIPTION_PURGE_GRACE_HOURS=24
REDIS_URL=redis://redis:6379/0
ADMIN_ID=change-me                 # числовой Telegram id админа (для модерации СБП)
SBP_PHONE_NUMBER=+7...
GMAIL_APP_PASSWORD=change-me
PAYMENT_AMOUNT=150
PAYMENT_DAYS=30
JWT_SECRET=change-me-long-random-string   # длинный случайный; один и тот же у bot и admin
# ЛК: на проде укажи публичный https-поддомен, на который указывает A-запись:
#   CABINET_URL=https://cab.nyxvps.space  (смени `cab.nyxvps.space` на реальный поддомен)
#   Certbot: `DOMAIN=cab.nyxvps.space bash docker/scripts/issue_cert.sh` (после `docker compose up -d`)
# Участок /cabinet — публичный (JWT), /admin/* — под HTTP Basic.
CABINET_URL=https://cab.nyxvps.space
